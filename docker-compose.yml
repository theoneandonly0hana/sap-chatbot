services:
  sapbot:
    image: python:3.11-slim
    working_dir: /app
    ports:
      - "8088:8088"
    volumes:
      - ./:/app
    command: bash -lc "pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8088"
